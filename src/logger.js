const info = (message) => {
  console.log(message);
};

const debug = (message) => {
  console.log(message);
};

const warn = (message) => {
  console.log(message);
};

const error = (message) => {
  console.log(message);
};

module.exports = {
  info,
  error,
  warn,
  debug,
};
